language: java

sudo: true

before_script:
- sudo apt-get install tightvncserver

cache:
  directories:
  - $HOME/.m2

#skip installation step
install: true

jdk:
- oraclejdk8

script:
- script.sh mvn -f pom.xml clean verify